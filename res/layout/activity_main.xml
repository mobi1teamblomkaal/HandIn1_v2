<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <Button
        android:id="@+id/btnSecondActivity"
        android:layout_width="300dp"
        android:layout_height="200dp"
        android:layout_centerHorizontal="true"
        android:onClick="onClickToSecondActivity"
        android:text="To second activity" />

    <Button
        android:id="@+id/btnReorder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/btnSecondActivity"
        android:layout_below="@+id/btnSecondActivity"
        android:layout_marginRight="48dp"
        android:layout_marginTop="15dp"
        android:text="Reorder" 
        android:onClick="onClicnToSecondActivityReorder"/>

</RelativeLayout>